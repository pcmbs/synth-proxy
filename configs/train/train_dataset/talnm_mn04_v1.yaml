# path to the pkl dataset folder used for training
path: ${paths.root_dir}/data/datasets/talnm_mn04_size=10240000_seed=500_train_v1

loader:
  batch_size: 512
  num_workers: 8
  shuffle: True
  pin_memory: False
  drop_last: False

name: talnm_mn04_train_v1